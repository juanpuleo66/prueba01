import { Component } from '@angular/core';

@Component({
    selector: 'common-functions',
    template: ''
})

export class CommonFunctions {

    funcion1(valor){
        alert(valor);
    }
}
